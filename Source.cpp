#include"Point.h"
#include"PrintHelper.h"
#include"Logic.h"
#include"Snake.h"
int main()
{
    /*Point point{ 10, 10 };
    PrintHelper pr;
    while(true)
    {
        pr.Print(point, '*');
        char c = _getch();
        pr.Clear(point);
        change(point, c);
    }
    Snake s(point);*/
    Logic log;
    log.Run();
    return 0;
}